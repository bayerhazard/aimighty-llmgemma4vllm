apiVersion: v1
kind: Service
metadata:
  name: aimllmgemma4vllm
  namespace: {{ .Release.Namespace }}
  labels:
    app: aimllmgemma4vllm
    io.kompose.service: aimllmgemma4vllm
spec:
  type: {{ .Values.service.type }}
  ports:
  - name: "8000"
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
  selector:
    io.kompose.service: aimllmgemma4vllm
