apiVersion: apps/v1
kind: Deployment
metadata:
  name: aimllmgemma4vllm
  namespace: {{ .Release.Namespace }}
  labels:
    app: aimllmgemma4vllm
    io.kompose.service: aimllmgemma4vllm
  annotations:
    applications.app.bytetrade.io/entrances: '[{"name":"aimllmgemma4vllm","host":"aimllmgemma4vllm","port":8000,"icon":"https://raw.githubusercontent.com/bayerhazard/aimighty-llmgemma4vllm/main/icon.png","title":"AIM Gemma 4 26B A4B","authLevel":"internal","openMethod":"window"}]'
    applications.app.bytetrade.io/icon: https://raw.githubusercontent.com/bayerhazard/aimighty-llmgemma4vllm/main/icon.png
    applications.app.bytetrade.io/required_gpu: "1"
    applications.app.bytetrade.io/title: AIM Gemma 4 26B A4B
    applications.app.bytetrade.io/version: "26.08.23"
spec:
  replicas: {{ .Values.workloads.aimllmgemma4vllm.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      io.kompose.service: aimllmgemma4vllm
  template:
    metadata:
      labels:
        app: aimllmgemma4vllm
        io.kompose.network/chrome-default: "true"
        io.kompose.service: aimllmgemma4vllm
        applications.app.bytetrade.io/name: aimllmgemma4vllm
        applications.app.bytetrade.io/owner: aimighty
        applications.app.bytetrade.io/raw-app-name: aimllmgemma4vllm
        io.bytetrade.app: "true"
    spec:
      restartPolicy: Always
      dnsPolicy: ClusterFirst
      terminationGracePeriodSeconds: 30
      containers:
      - name: aimllmgemma4vllm
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command:
        - vllm
        - serve
        args:
        - $(MODEL_NAME)
        - --served-model-name
        - $(MODEL_ALIAS)
        - --host
        - 0.0.0.0
        - --port
        - "8000"
        - --max-model-len
        - $(MAX_MODEL_LEN)
        - --gpu-memory-utilization
        - $(GPU_MEMORY_UTILIZATION)
        - --max-num-seqs
        - {{ .Values.env.MAX_NUM_SEQS | quote }}
        - --max-num-batched-tokens
        - {{ .Values.env.MAX_NUM_BATCHED_TOKENS | quote }}
        - --attention-backend
        - triton_attn
        - --kv-cache-dtype
        - fp8
        - --dtype
        - auto
        - --trust-remote-code
        - --download-dir
        - /models/huggingface
        - --enable-prefix-caching
        - --limit-mm-per-prompt
        # video:0 explizit — nightly-Profiler ignoriert fehlende video-Key und allokiert
        # sonst 6 video items max feature size (~3 GiB VRAM) beim MM-Profiling.
        - '{"image":1,"video":0}'
        - --enable-auto-tool-choice
        - --tool-call-parser
        - gemma4
        - --reasoning-parser
        - gemma4
        - --default-chat-template-kwargs
        - '{"enable_thinking": true}'
        - --generation-config
        - vllm
        # MTP/Draft-Experiment (26.08.26): google/gemma-4-26B-A4B-it-assistant
        # (4-Layer-MTP-Drafter, ~420 MB bf16). Fruehere Fehler (mat-shape 3840 vs
        # 5632, CUDAGraph-OOM) waren v0.26.0 ohne Fixes; jetzt #47091 + #48892 +
        # #49797 im Nightly ac7509e2. num_speculative_tokens: n_predict=None => gesetzt.
        - --speculative-config
        - '{"method":"mtp","model":"google/gemma-4-26B-A4B-it-assistant","num_speculative_tokens":3}'
        envFrom:
        - configMapRef:
            name: aimllmgemma4vllm-env
        env:
        - name: HF_HOME
          value: /models/huggingface
        - name: HF_TOKEN
          value: {{ .Values.olaresEnv.HF_TOKEN | default "" | quote }}
        - name: MODELS_DIR
          value: /models
        - name: CUDA_DEVICE_MEMORY_LIMIT_0
          value: "24400m"
        - name: GPU_TYPE
          value: nvidia
        - name: VLLM_USE_FLASHINFER_SAMPLER
          value: "0"
        ports:
        - containerPort: {{ .Values.service.port }}
          protocol: TCP
        resources:
          limits:
            cpu: {{ .Values.resources.limits.cpu }}
            memory: {{ .Values.resources.limits.memory }}
            nvidia.com/gpu: {{ index .Values.resources.limits "nvidia.com/gpu" }}
          requests:
            cpu: {{ .Values.resources.requests.cpu }}
            memory: {{ .Values.resources.requests.memory }}
            nvidia.com/gpu: {{ index .Values.resources.requests "nvidia.com/gpu" }}
        volumeMounts:
        - name: models
          mountPath: /models
        startupProbe:
          httpGet:
            path: {{ .Values.probes.startup.path }}
            port: {{ .Values.probes.startup.port }}
          failureThreshold: {{ .Values.probes.startup.failureThreshold }}
          periodSeconds: {{ .Values.probes.startup.periodSeconds }}
          initialDelaySeconds: {{ .Values.probes.startup.initialDelaySeconds }}
          timeoutSeconds: {{ .Values.probes.startup.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: {{ .Values.probes.liveness.path }}
            port: {{ .Values.probes.liveness.port }}
          failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
          timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
      volumes:
      - name: models
        hostPath:
          path: {{ .Values.userspace.appData }}/Data/aimllmgemma4vllm/models
          type: DirectoryOrCreate
