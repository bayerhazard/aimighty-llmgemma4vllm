---
olaresManifest.version: '0.12.0'
olaresManifest.type: app
apiVersion: 'v3'

workloadReplicas:
  aimllmgemma4vllm: 1

metadata:
  name: aimllmgemma4vllm
  icon: https://raw.githubusercontent.com/bayerhazard/aimighty-llmgemma4vllm/main/icon.png
  description: Gemma 4 26B A4B multimodal via vLLM — QAT-AWQ INT4, 200K context, vision
  appid: aimllmgemma4vllm
  title: AIM Gemma 4 26B A4B
  version: '26.08.29'
  categories:
    - AI
    - Vision
entrances:
  - name: aimllmgemma4vllm
    port: 8000
    host: aimllmgemma4vllm
    title: AIM Gemma 4 26B A4B
    icon: https://raw.githubusercontent.com/bayerhazard/aimighty-llmgemma4vllm/main/icon.png
    openMethod: window
    authLevel: internal
spec:
  versionName: '26.08.29'
  upgradeDescription: |
    v26.08.29: Production-Haertung — GPU_MEMORY_UTILIZATION 0.98->0.96
    (Sicherheitspuffer: 0.98 liess nur ~90 MB frei, CUDACachingAllocator-OOM
    beim Warmup). KV-Pool ~4.1-4.2 GiB reicht fuer 200K (~3.57 GiB Bedarf).
    VLLM_EXTRA_ARGS entfernt (toter Code, generation-config ist hart in args).
    Built for Olares 1.12.6.
  fullDescription: |
    Gemma 4 26B A4B — multimodal LLM (text + image) with QAT-AWQ-INT4 quantization on vLLM.

    **Model**
    cyankiwi/gemma-4-26B-A4B-it-qat-AWQ-INT4 (QAT + AWQ INT4, ~16 GB).
    200K token context. Multimodal (text + image, no video).

    **Inference Engine**
    vLLM nightly 2026-08-14 cu129, SHA-pinned (cu129-nightly-ac7509e2) with triton_attn backend. CUDA 12.9 (RTX 5090 Blackwell).
    fp8 KV-Cache (all layers). CUDAGraphs + torch.compile.
    Native hybrid SWA: 5 full-attention layers for long context, 25 sliding-window layers bounded.
    GPU VRAM: ~22.5 GB.

    **Performance (RTX 5090 Blackwell)**
    - 200K token context
    - ~136 tok/s generation
    - Needle Haystack: 25/25 (100%)
    - Agentic Tool-Calling: 8/8 (100%)

    **API**
    OpenAI-compatible: /v1/chat/completions, /v1/models, /health.
    Tool Calling via --enable-auto-tool-choice + --tool-call-parser gemma4.
    Reasoning via --reasoning-parser gemma4 + --default-chat-template-kwargs enable_thinking.

    **Resource Usage**
    GPU: ~22.5 GB VRAM (RTX 5090, 24 GB)
    RAM: 24-40 GB
    Disk: 50 GB (model download ~16 GB + cache)
    CPU: 4-16 cores

  developer: Aimighty
  website: https://github.com/bayerhazard/aimighty-llmgemma4vllm
  sourceCode: https://github.com/bayerhazard/aimighty-llmgemma4vllm
  locale:
    - en-US
  supportArch:
    - amd64
  requiredCpu: 4
  requiredMemory: 24Gi
  limitedCpu: 16
  limitedMemory: 40Gi
  requiredDisk: 50Gi
  requiredGpu: 1
permission:
  appData: true
envs:
  - envName: HF_TOKEN
    required: false
    applyOnChange: true
    valueFrom:
      envName: OLARES_USER_HUGGINGFACE_TOKEN
options:
  apiTimeout: 3600
  dependencies:
    - name: olares
      version: '>=1.12.6-0'
      type: system
  allowMultipleInstall: false
