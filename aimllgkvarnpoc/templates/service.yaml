apiVersion: v1
kind: Service
metadata:
  name: aimllgkvarnpoc
  namespace: {{ .Release.Namespace }}
  labels:
    app: aimllgkvarnpoc
    io.kompose.service: aimllgkvarnpoc
spec:
  type: {{ .Values.service.type }}
  ports:
  - name: "8000"
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
  selector:
    io.kompose.service: aimllgkvarnpoc
